# MeaningForge v15 / v12 / 用户实验实现核查

核查日期：2026-08-26

## 结论

三份文档在“读者拥有解释权、系统只提供可质疑且可回到原文的支架”这一核心定位上基本一致。主要不一致来自系统能力和实验材料被写成了完成态，但仓库当前只实现了其中一部分。

## 对齐矩阵

| 研究与设计要求 | 用户实验中的操作化 | 当前实现状态 |
| --- | --- | --- |
| DP1 可拒绝线索 | 接受、拒绝幽灵建议 | 已实现参考关系的保留、拒绝、修改及个人替代关系 |
| DP2 证据锚定 | TextSpan 引用、跨文本连线 | 已实现冻结原文、Evidence、TextSpan 和读者关系证据 ID |
| DP3 地平线原则 | 不暴露未读内容 | 已实现会话级已读章节集合；候选与参考关系只使用已进入章节，手动进入新章节后才扩大地平线 |
| DP4 读者层独立 | 墨点、连线、个人总结 | 已实现独立 ReaderNode、ReaderRelation、ReaderClaim |
| DP5 渐进披露 | 四层展开 | 已形成概览、候选展开、证据联动、读者意义组织四级路径；各次展开与视图切换进入日志 |
| DP6 多义并存 | 多个读法共存 | 已实现并行个人关系、置信度、历史与反证 |
| DP7 安静陪读 | 幽灵可隐藏 | MeaningForge 默认主动显示 3–5 条轻量幽灵；不弹窗、不抢焦点，读者可全局隐藏或逐条拒绝；结构仍按需展开 |
| DP8 加法/减法 | 信号支架与背景知识分离 | 本轮补齐背景知识卡；MIP/UNR 与知识请求保持不同数据路径 |
| 强化基线 | 单栏原文、线性笔记、同一知识后端 | 本轮新增 `condition=baseline`，并按参与者、轮次、条件隔离数据 |
| 行为日志与导出 | 锚定、连线、接受率、查询、修订 | 已有事件流；本轮加入实验上下文、查询与基线笔记修订记录 |
| 四类协调视图 | 图谱、时空、解读空间、联动 | 已实现四个独立读者视图的基础工作流；时空视图只显示真实事件与场景，尚无拖拽重排、缩放、校准情感曲线和人物移动轨迹 |
| 两篇正式材料 | 《药》与《竹林中》 | 仅《药》具备冻结参考包；《竹林中》尚未准备 |

## 技术可实现性

- WorkPackage、来源锚定、引用完整性验证、读者层隔离、事件日志和本地导出均可实现，当前已有可运行基础。
- MIP/MIPVU 是人工判定程序，不应把规则扫描或 LLM 输出直接称为自动完成的 MIP/MIPVU。当前实现较合理地保留了 `machine_draft`、`machine_reviewed`、`researcher_checked` 状态，但正式材料仍需要人工复核。
- v12 所称 UNR 在文档中缺少可复现的算法定义、阈值、训练或规则来源。当前实现实际是确定性重复、对照、异常与跨段候选抽取；论文和界面应使用这一可审计名称，除非另行补充 UNR 的正式定义。
- 仅靠 LLM Prompt 和关键词过滤无法保证事实正确、来源真实。知识卡可作为研究者预审草稿；正式实验应冻结经过核验的知识响应或接入可引用检索源。
- 四视图可以工程实现，但时空轨迹需要可靠的事件、时间、地点、共指数据；解读空间需要明确维度来源。两者目前没有足以支持正式实验的校准数据，不宜用占位可视化补齐。

## 本地实验入口

```text
/?condition=meaningforge&participant=P01&session=round-1
/?condition=baseline&participant=P01&session=round-2
```

两种条件使用同一背景知识接口，但浏览器存储键和导出文件按参与者、轮次与条件隔离。正式实验前仍需加入实验阶段锁定、计时、匿名编号校验和研究者统一导出流程。
